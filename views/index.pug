h2 Agregar Ficheros al SERVER
div
    form(action="/subir", method="POST", enctype="multipart/form-data")
        label Imagen
        input(type="file", name="file")
        input(type="submit", value="Enviar")
        br
        a(href='descargar') DESCARGAR